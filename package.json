{
  "name": "mizupass-backend",
  "version": "1.0.0",
  "description": "MizuPass backend API using Hono and ZKPassport SDK",
  "main": "dist/index.js",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=bun --external=@zkpassport/sdk",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@zkpassport/sdk": "0.8.3",
    "dotenv": "^17.2.2",
    "hono": "^4.0.0",
    "pinata-web3": "^0.5.4",
    "viem": "^2.37.5"
  },
  "devDependencies": {
    "@types/bun": "^1.0.0",
    "typescript": "^5.0.0"
  },
  "type": "module"
}